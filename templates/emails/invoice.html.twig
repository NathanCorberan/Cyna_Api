<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Facture n°{{ invoice_id }}</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .header { margin-bottom: 20px; }
        .footer { margin-top: 40px; font-size: 12px; color: #777; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        table, th, td { border: 1px solid #ccc; }
        th, td { padding: 8px; text-align: left; }
    </style>
</head>
<body>
    <div class="header">
        <h2>Facture n°{{ invoice_id }}</h2>
        <p>Date : {{ date }}</p>
        <p>Client : {{ customer_email }}</p>
    </div>

    <table>
        <thead>
            <tr>
                <th>Description</th>
                <th>Quantité</th>
                <th>Prix unitaire</th>
                <th>Total</th>
            </tr>
        </thead>
        <tbody>
            {% for item in items %}
                <tr>
                    <td>{{ item.description }}</td>
                    <td>{{ item.quantity }}</td>
                    <td>{{ item.unit_price }} €</td>
                    <td>{{ item.total_price }} €</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <h3>Total à payer : {{ total_amount }} €</h3>

    <div class="footer">
        <p>Merci pour votre achat !</p>
        <p>Cyna - contact@cyna.fr</p>
    </div>
</body>
</html>
